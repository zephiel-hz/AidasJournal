{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "corepack pnpm install --frozen-lockfile"
      ]
    },
    "build": {
      "inputs": [
        {
          "step": "install"
        }
      ],
      "commands": [
        "VITE_ASSET_VERSION=$(git rev-parse --short HEAD 2>/dev/null || date +%s) PORT=3000 BASE_PATH=/ corepack pnpm --filter @workspace/aida run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "corepack pnpm --filter @workspace/aida run serve -- --port $PORT",
    "variables": {
      "BASE_PATH": "/"
    }
  }
}
